---
name: CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  
jobs:
  ci:
    name: CI
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - run: echo "it works"
